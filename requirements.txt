# Clarity OMR dependencies
# Install: pip install -r requirements.txt
#
# For GPU (CUDA) support, install PyTorch with CUDA first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Then install the rest:
#   pip install -r requirements.txt
#
# For CPU only (PyTorch CPU is installed automatically):
#   pip install -r requirements.txt

# PyTorch (CPU wheels - override with CUDA install above if you have a GPU)
torch
torchvision

# Model download
huggingface_hub

# PDF rendering (install at least one)
pymupdf

# Stage A - staff detection
ultralytics

# Stage B - token recognition
einops
timm
transformers
peft

# MusicXML export
music21
lxml

# Image processing
# Pillow pinned to 12.1 — 12.2.0 introduces a ~2x cpu_encode regression
# (image decode/resize) measured during the cu132 bench (Phase 3). The
# regression is hidden by the DataLoader's worker prefetch on the default
# --num-workers 4 path, but resurfaces with --num-workers 0 (Windows fallback).
# See docs/perf/2026-04-27-cu132-rollout.md "follow-ups" section.
Pillow~=12.1.0
numpy
scikit-image

# Config
PyYAML

# Evaluation (optional)
# mir_eval
# verovio
# cairosvg
zss  # Zhang-Shasha tree edit distance (used by eval/tedn.py)
psutil  # System memory polling for adaptive TEDN throttle (eval/score_*_eval.py)
