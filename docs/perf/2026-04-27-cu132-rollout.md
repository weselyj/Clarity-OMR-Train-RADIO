# cu132 Rollout Decision (2026-04-27) — DRAFT

This is the Phase 7 destination doc. Filled in incrementally as Phase 3 → 4.2 → 5.2 → 6 finish.

Plan: [`docs/superpowers/plans/2026-04-27-pytorch-nightly-cu132.md`](../../docs/superpowers/plans/2026-04-27-pytorch-nightly-cu132.md).
Pre-cu132 baseline: [`docs/perf/2026-04-27-baseline-pre-cu132.md`](2026-04-27-baseline-pre-cu132.md).

## Toolchain summary

| Item | Value |
|---|---|
| GPU | NVIDIA RTX 5090 (32 GB, WDDM) |
| Driver | 596.21 (CUDA Version 13.2 exposed) |
| Old wheel | torch 2.11.0+cu128 (Python 3.13) |
| New wheel | torch 2.13.0.dev20260426+cu132 (Python 3.13) |
| cuDNN | 9.21.01 (via nvidia-cudnn-cu13 wheel) |
| Workaround | `sitecustomize.py` adds `nvidia/cu13/bin/x86_64` and `nvidia/cudnn/bin` to both `os.add_dll_directory` and `os.environ["PATH"]` |

## Bench results

### Phase 3 — cu132 baseline (700 opt-steps Stage 2, no compile)

_TBD — fill from `bench/cu132_700/profile.jsonl` via `scripts/profile_summary.py --warm 200`_

| Phase | Mean ms | % of wall |
|---|---|---|
| gpu_backward | TBD | TBD |
| gpu_forward | TBD | TBD |
| gpu_grad_diagnostics | TBD | TBD |
| cpu_augment | TBD | TBD |
| cpu_encode | TBD | TBD |
| cpu_h2d | TBD | TBD |
| **Wall mean** | **TBD** | — |

### Phase 4.2 — cu132 + torch.compile (decoder + bridge only)

_TBD — fill from `bench/cu132_compile/profile.jsonl`_

| Phase | Mean ms | % of wall |
|---|---|---|
| Wall mean | TBD | — |
| Δ vs Phase 3 | TBD | — |

## Loss-curve regression check (Phase 5.2)

_Comparison: `logs/full_radio_stage2_steps.jsonl` (cu128 reference) vs `bench/cu132_700/steps.jsonl` (cu132 baseline)_

```
$ python scripts/compare_step_logs.py \
    --baseline bench/cu128_reference_steps.jsonl \
    --candidate bench/cu132_700/steps.jsonl \
    --rel-tol 0.10
```

| Field | Value |
|---|---|
| max_relative_diff | TBD |
| compared_steps | TBD |
| skipped_zero_baseline | TBD |
| regressed | TBD |

## Summary table

| Configuration | Wall (ms/opt-step) | Encoder fwd (ms) | Encoder bwd (ms) | Δ vs cu128 baseline |
|---|---|---|---|---|
| **cu128 reference** (issue #2 pinned comment, 250 measured) | 5362 | 1828 | 3255 | — |
| **cu132 nightly** (Phase 3, 500 measured, warm 200) | TBD | TBD | TBD | TBD% |
| **cu132 + torch.compile** (Phase 4.2, 500 measured, warm 200) | TBD | TBD | TBD | TBD% |
| cu132 + DataLoader rewrite (Phase 6, only if dispatched) | n/a — gated | — | — | — |

## Phase 6 decision (DataLoader gate)

_TBD — applied after Phase 3 results land. Rule from the plan:_

| `cpu_pipeline_pct` from Phase 3 | Action |
|---|---|
| < 5% | Skip Phase 6. |
| 5–10% | Skip Phase 6 (plan author's recommendation). |
| > 10% | Dispatch the DataLoader rewrite as a separate PR (issue #2 Tier 1 #1+#2). |

## Decision

_TBD — one of:_

- **ADOPT:** rename `venv` → `venv-pre-cu132`, `venv-cu132` → `venv`. Tag `pre-cu132-rollback`. Update `NOTES.md` + `docs/TRAINING_COMMANDS.md`. Open follow-up PR.
- **KEEP-OPT-IN:** leave both venvs. Document `venv-cu132/` as recommended for future runs.
- **ROLLBACK:** delete `venv-cu132/`. Document why. The work informed the team about which optimizations are dead ends.

## Notes on the data restore mishap

During the cu132 work, an unintended Junction follow during a `git worktree remove --force` wiped `data/` on the host (tracked in `docs/perf/2026-04-27-baseline-pre-cu132.md` and the session log). All five datasets were re-fetched from public sources via `scripts/restore_data_inner.ps1` plus `restore_data_launch.ps1`. NAS backup at `\\10.10.1.234\Share\datasets\clarity-omr-data\` via `scripts/backup_data_to_nas.ps1`. Synthetic_full_page still pending regeneration (4 h CPU job).
